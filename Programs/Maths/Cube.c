#include <stdio.h>
#include <conio.h>

int main()
{
	int a,c;
	printf("\n Enter the number ");
	scanf("%d", &a);
	c = a*a*a; // Example 2*2*2 = 8 , it's used in this expression.
	printf("\n Cube of number %d ",c);
	return 0;
}